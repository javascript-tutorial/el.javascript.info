importance: 5

---

# Ξαναγράψτε 'if' μέσα στο '?'

Ξαναγράψτε τό `if` χρησιμοποιώντας τον τελεστή υπό όρους `'?'`:

```js
let result;

if (a + b < 4) {
  result = 'Below';
} else {
  result = 'Over';
}
```
